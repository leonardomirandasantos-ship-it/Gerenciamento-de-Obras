# 03 — Decisões (Decision Log D1–D162) + Escopo V1/V2

> **Este arquivo é normativo.** Em caso de ambiguidade na implementação, estas
> decisões têm prioridade. Cada decisão nasceu do refinamento de produto e/ou da
> análise de dado real (export de conversa de WhatsApp) e das telas geradas.

---

## 1. Decision Log completo

### Fundamentos e captura
| # | Decisão | Status |
|---|---------|--------|
| D1 | Sem gate de confirmação na captura | ✅ |
| D2 | Classificação em 2º plano; correção como mecanismo principal | ✅ |
| D3 | Nada é descartado (inclui "não classificado") | ✅ |
| D4 | Comprovante: junto OU anexado depois | ✅ |
| D5 | Comprovante/NF também no gasto (evidência) | ✅ |
| D6 | Chat é a única superfície de captura; dashboards só leem. O atalho "+" da lente não abre exceção: ele escreve na conversa (ver D79) | ✅ |
| D7 | Não congelar fluxos/design antes da análise do export (feito depois) | ✅ |
| D8 | 4º tipo (atualização/documentação) capturado na V1 | ✅ |

### Inteligência (Fluxo 2)
| # | Decisão | Status |
|---|---------|--------|
| D9 | Inteligência = sugestão de 1 toque, opcional, no momento certo (5 leis) | ✅ |
| D10 | Reembolso tem 2 tempos (marcar + baixa) — **movido para V2** (ver D41) | ↪️ V2 |
| D11 | Pendências com prazo formam calendário emergente | ✅ |
| D12 | "Fechar o dia" entra na V1 como lente leve | 🟡 |
| D13 | Toda lente só LÊ eventos — nenhuma lente cria registro próprio. O atalho de captura da lente grava na conversa e a lente lê de lá (ver D79) | ✅ |

### Pós-análise do export real (pivô logística-first)
| # | Decisão | Status |
|---|---------|--------|
| D14 | Herói da V1 = logística (lista + checklist + documentação) | ✅ |
| D15 | Captura agnóstica ao tipo; diferenciação só no 2º plano | ✅ |
| D16 | Checklist vivo é tipo próprio (E2), com fusão de re-envios | ✅ |
| D17 | Excluir = trivial, inclusive em lote | ✅ |
| D18 | Sugestão de data é oportunista (só quando cita data), não central | ✅ |
| D19 | Voz: transcrição sob demanda na V1 | ✅ ver D124 |
| D20 | Foto nunca exige legenda; busca por data/fase | ✅ (ver D31) |

### Engine de sugestão
| # | Decisão | Status |
|---|---------|--------|
| D21 | Toda inteligência = 1 engine de sugestão reutilizável | ✅ |
| D22 | Componente único "cartão de sugestão" para todos os casos | ✅ |
| D23 | Casos V1: A (checklist), B (status), C (data leve), D (decisão), E (PIX) | ✅ |
| D24 | Casos de reembolso → V2 | ↪️ V2 |
| D25 | Anti-irritação: silenciar após ignorar 2×; nunca rotina diária | ✅ |

### Financeiro validado (rodada 2 de insights do usuário)
| # | Decisão | Status |
|---|---------|--------|
| D26 | Financeiro (E7) validado como dor real; entra no roadmap V1 | ✅ |
| D27 | "Host de pagamentos": app é destino do encaminhamento de comprovante | ✅ |
| D28 | Sumário financeiro por fase e por prestador/fornecedor | ✅ |
| D29 | Orçamento é evento próprio (E8), buscável por fornecedor/produto | ✅ |
| D30 | Materiais consultáveis por categoria | ✅ |
| D31 | Foto: legenda opcional (na hora ou depois), nunca obrigatória (reconcilia D20) | ✅ |
| D32 | Tag de fase é global/transversal, aplicável na hora ou depois em 1 toque | ✅ |
| D33 | Documentação = diário de obra pesquisável (foto+legenda+fase+orçamento) | ✅ |

### Fechamento de escopo (validação de completude)
| # | Decisão | Status |
|---|---------|--------|
| D34 | Criar obra estilo "grupo WhatsApp": foto + nome + opcionais | ✅ |
| D35 | Cadastro/aceitar convite: NÃO na V1 (login simples; link por fora) | ✅ |
| D36 | Convidar pessoas: manual/externo, sem tela no app na V1 | ✅ |
| D37 | Menu "3 pontos": resolvido sem menu. O header inteiro da obra leva às Configurações (D96) e cada obra na home tem engrenagem (D121) — um destino direto em alvo grande valeu mais que uma lista de opções escondida | ✅ |
| D38 | Editar/reclassificar/excluir evento entra na V1 (via bottom sheet) | ✅ |
| D39 | "Decisões da obra" entra na V1 | ✅ |
| D40 | Papéis/permissões: 1 usuário master; sem segregação na V1 | ✅ |
| D41 | Reembolso (JC-3): **NÃO na V1 → V2**. Foco: pagar prestador + saber gasto | ✅ |
| D42 | Offline-first: não é requisito da V1 (assume internet ao abrir) | ✅ |
| D43 | Motor de classificação: LLM via API desde a V1 (texto); PIX visão + confirmação | ✅ |
| D44 | Plano B da IA: não classificou → nada; classificou errado → usuário edita | ✅ |

### Navegação e consistência visual (decisões tomadas vendo as telas)
| # | Decisão | Status |
|---|---------|--------|
| D45 | **Sem navbar inferior global.** Cada obra é um "grupo"; lentes acessadas por **abas no topo** (Resumo · Conversa · Pendências · Documentação · Decisões — eram 6, ver D94). Preserva contexto por obra e o input do chat embaixo | ✅ |
| D46 | **Bottom sheet reutilizável**: "Editar registro" é componente global; qualquer "editar" abre o mesmo sheet | ✅ |
| D47 | Campo de **localização/cidade** na criação da obra (opcional) → alimenta o subtítulo do header ("Obra Ecologie · São Paulo, SP") | ✅ |

---

## 2. Decisões técnicas de classificação (detalhe de D43/D44)

Contexto do build inicial: **sem backend estruturado**, projeto em **git público**,
objetivo de **testar o conceito**. Caminhos (do mais simples ao mais rico):

1. **Regras/palavras-chave (recomendado começar aqui):** tem imagem → foto; várias
   linhas → lista; contém "R$"/"pix"/"comprovante" → pagamento; contém data/
   "comprar" → lembrete. **Zero custo, zero chave, funciona offline.**
2. **LLM no navegador** (Transformers.js / WebLLM): classifica texto localmente,
   **sem chave e sem backend** — ideal para git público. Modelo baixa no 1º uso.
3. **API de LLM com tier gratuito** (Gemini / Groq / OpenRouter): mais inteligente,
   **mas exige chave** → só com um mini-proxy serverless (Vercel/Netlify) para não
   expor a chave no front público.

> **Regra de segurança:** NUNCA colocar chave de API no front público. Ver
> `07_ARQUITETURA_TECNICA.md`. O plano B (D44) garante que erros de classificação
> tenham custo baixo, então começar por regras é seguro.

---

## 3. Escopo — V1 vs V2

### ✅ Dentro da V1
- Múltiplas obras (usuário master único).
- Conversa de captura (texto, foto, voz, PDF) sem gate.
- Classificação em 2º plano + "não classificado" + edição (bottom sheet).
- Eventos E1–E8; logística como herói.
- **Financeiro via Host de pagamentos**: reconhecer favorecido, agrupar por
  pessoa, sumário por fase e por prestador/fornecedor.
- Lentes: Checklists, Documentação (diário, foto+legenda opcional+fase),
  Decisões, Dash da obra, Prestador (deep-dive), Orçamentos.
- Fases: 5 fixas (Fundação · Estrutura · Hidráulica · Elétrica · Acabamento) +
  editáveis/reordenáveis.
- Navegação por abas no topo da obra (sem navbar global).
- Login simples (e-mail/senha + Google); acesso por link enviado por fora.

### ❌ Fora da V1 (→ V2 ou depois)
- **Reembolso / "a receber" / cobrança do dono** (D41).
- **Papéis e permissões** / segregação de acesso (D40).
- **Dash geral** (portfólio de todas as obras).
- **Comparação de orçamentos** (não validado como dor).
- **Cadastro/aceitar convite** dentro do app e **convidar pelo app** (D35/D36).
- **Notificações ricas**; começar simples ou depois.
- **Offline-first** (D42).
- **Prestação de contas formal ao dono** (relatório por fase).
- Menu "3 pontos" detalhado (D37).

---

## 4. Pendências conscientes (não bloqueiam a V1)

1. Validar com **export de 1 conversa 1:1 de pagamento** (refina E7/Caso E).
2. Confirmar libs de IA no build (regras → WebLLM → API). → ver D55.
3. ~~Limiar de similaridade do "checklist vivo"~~ → resolvido em D51.
4. Definir gatilhos de "fase" que disparam notificação.

---

## 5. Decision Log — decisões tomadas durante o build da V1 (D48–D57)

| # | Decisão | Status |
|---|---------|--------|
| D48 | **Persistência = Supabase** (Postgres + Auth + Storage), não IndexedDB local como sugeria o `07`. Motivo: ela usa **dois celulares**, e o dado precisa existir nos dois; o Storage também resolve foto/comprovante. O `07` já previa adaptação pela IA de código | ✅ |
| D49 | Schema **single-user** de verdade: sem empresa/convite/papéis. Todo dado pertence ao `owner_id = auth.uid()`, com RLS por dono da obra. Conta criada no painel do Supabase (coerente com D35/D36) | ✅ |
| D50 | Reordenar fases por **setas ▲▼**, não drag-and-drop. Motivo: drag em web mobile exige lib e quebra fácil no toque; a função é a mesma | ✅ |
| D51 | **Limiar do checklist vivo (Caso B):** sugere fusão quando **2+ itens casam E ≥50% de sobreposição** (após normalizar acento, status e número). Conservador de propósito: erra pra menos, não pra mais | ✅ |
| D52 | **Ordem de classificação:** texto com 3+ linhas é lista (sinal mais forte do uso real, ~40% do texto) e ganha de "decisão". O **nome do arquivo** entra na classificação (ex.: "Orçamento 335396.pdf" → E8) | ✅ |
| D53 | **Excluir = soft delete** (`deleted = true`), nunca apaga fisicamente — mantém "toda ação é reversível". Exclusão **em lote** (D17) ainda não implementada | 🟡 |
| D54 | **Orçamentos não tem aba própria** (as abas são as do D45): é acessada pelo card no Resumo. *Revisto no D148: mora na metade "Arquivos" da Documentação; o card do Resumo virou atalho para lá* | ✅ |
| D55 | **OCR de comprovante ainda não implementado.** Enquanto isso, o caminho é manual: editar o registro e informar valor + favorecido no bottom sheet. Pagamento por **texto** ("pix de R$ 1.250 pro José Costa") já é extraído automaticamente. Decisão pendente com o dono: **decidido contra o Tesseract.js**: são ~10MB baixados no celular e ele lê mal foto de papel. Como o D126 já traz um modelo com visão para o áudio, ler o comprovante por visão sai mais simples e mais robusto — e ainda pega nota fiscal, que o Tesseract não pegaria. **Implementado** via `/api/entender` — ver D134 | ✅ |
| D56 | Captura de voz — **implementada**, ver D124/D125. O cliente inverteu nossa premissa: ele não manda áudio hoje **porque depois não acha**, que é exatamente o problema que o app resolve. Áudio deixou de ser conveniência e virou a captura de menor esforço | ✅ |
| D57 | **Tema único, sem dark mode.** O template inicial invertia as cores pelo `prefers-color-scheme` e quebrava o contraste dos botões; agora a paleta do `05` vale sempre | ✅ |
| D76 | **Lista e checklist são o MESMO card, numa seção só.** Separar em "Listas recebidas" e "Checklists" fazia o card sumir do lugar ao ser convertido e reaparecer no fim da página — com várias listas, parecia que "criou a primeira e parou de criar" (os checklists estavam sendo criados, só fora da tela). Agora o card converte no lugar, mantendo posição e ordenação pela data da lista de origem | ✅ |
| D77 | **Card tem ação de conjunto além do item:** "✓ Concluir tudo" e "🗑 Tirar da lista", sem tirar o checkbox por item | ✅ |
| D78 | **"Tirar da lista" remove das pendências, não apaga a mensagem** (marca `dismissed`): a conversa continua sendo o registro, e nada que ela mandou se perde (D3) | ✅ |
| D133 | **"Prestador ou fornecedor?" é silenciada por PESSOA, não por evento.** Depois de categorizar o Vilmar num pagamento, os outros pagamentos dele continuavam perguntando a mesma coisa — porque a engine checava só se AQUELE evento tinha tipo. Agora ela recebe os favorecidos já tipados da obra, e aceitar a sugestão preenche o tipo em **todos** os pagamentos daquela pessoa. Pergunta respondida uma vez não volta | ✅ |
| D134 | **Imagem passa pela IA e o comprovante É o gasto.** O comprovante não vira registro separado: o próprio evento da foto passa a ser o pagamento, com valor e favorecido lidos da imagem — é o D5 (comprovante também no gasto, como evidência). Assunto a mais no mesmo arquivo vira registro derivado apontando de volta, igual no áudio. Duas travas: só roda em imagem (PDF segue pelo nome do arquivo) e **só quando ela não declarou o tipo** — se ela disse que é foto de obra, não cabe à IA discordar. Faltando valor ou favorecido, a sugestão de pagamento incompleto que já existia pede o que falta, o que cobre o "pedir para confirmar" sem inventar tela nova | ✅ |
| D142 | **A foto pode voltar para "sem fase".** Uma vez posta numa fase, não havia como tirar: o visor só listava as fases, e o cliente às vezes quer a foto só em "Todas". ~~Chip "Sem fase"~~ (revisto no D145) — tocar na fase já marcada desmarca — o gesto de qualquer seleção. Junto veio um bug antigo: ao trocar de foto pelas setas, o visor **herdava a fase e a legenda da foto anterior**, porque o componente não era recriado; corrigido com uma `key` por foto | ✅ |
| D143 | **Criar fase onde ela faz falta.** Além das Configurações, a barra de filtro da Documentação ganhou um "+" e o visor de foto um "+ nova fase" — e a fase criada no visor já recebe a foto, porque se ela criou ali é para aquela foto. O "+" da barra fica **antes** de "Todas": no fim, com muitas fases, sumiria para fora da tela. Criação centralizada em `lib/fases.ts` para cor e ordem saírem iguais nos três lugares | ✅ |
| D145 | **"Todas" e "Sem fase" viram uma coisa só: "Todas".** Revisa o D142. Eram dois nomes para o mesmo estado, e ainda por cima assimétricos: o filtro tinha os dois, o visor só "Sem fase". Tirar a foto de uma fase agora é só tocar de novo na fase marcada — a foto fica sem fase e continua aparecendo em "Todas". Quem quiser um balde explícito cria uma fase chamada "Sem fase". Sem o chip, a dica "toque de novo para tirar" ao lado do rótulo é o que ensina o gesto | ✅ |
| D162 | **A quebra lê o começo da linha, não a linha inteira.** Refina o D159, que exigia que TODAS as palavras fossem limpas: "Comprar prego martelo e marreta que vem" não quebrava porque um "que vem" no fim cancelava tudo. Agora vale o trecho **inicial** de palavras simples — ele é a relação — e o rabicho gruda no último item ("prego", "martelo", "marreta que vem"). Efeito colateral aceito: "comprar cimento e areia para o contrapiso" passa a virar "cimento" + "areia para o contrapiso", que são duas compras mesmo | ✅ |
| D161 | **Trocar o prazo da lista limpa a data que o item herdou.** "comprar cal pra hoje" dava a mesma data para a lista e para o item; ao editar o prazo, a cópia velha ficava no item e o card mostrava **duas datas** — a original e a editada — e o app continuava cobrando pela antiga, então a obra nem aparecia como atrasada na tela inicial. Agora, ao salvar, some a data do item que era igual ao prazo anterior. Data que ela escreveu diferente da lista ("rejunte cinza 20/09" numa lista para 30/09) continua valendo | ✅ |
| D160 | **A lista crua vira checklist UMA vez só.** Regressão criada pelo D152: ao tornar o check otimista eu tirei a trava que o `disabled` fazia, e cada toque numa lista ainda não convertida disparava uma conversão — três toques rápidos deixavam três checklists iguais no banco e três cards iguais na aba. Agora a conversão é agendada, sai 500 ms depois do último toque e leva os itens **no estado final** (mandar só o índice do primeiro perdia os outros dois). Para o estrago já gravado, `listasAbertas` passa a mostrar um checklist por lista de origem: vale o que a lista aponta em `linkedChecklistId`, senão o mais novo | ✅ |
| D159 | **A quebra em itens segue o jeito que ela escreve, não a gramática.** Revisa o D156, que exigia vírgula **e** conector — no celular ninguém digita vírgula, e a regra ficou bonita e inútil: "Comprar areia pedra e brita" não quebrava. Três portas, da mais larga para a mais estreita: (1) vírgula + conector; (2) **verbo de aquisição** no início (comprar, pedir, encomendar, orçar, cotar) seguido só de palavras simples — depois de "comprar" vem uma relação de coisas, e é por isso que o verbo precisa ser de aquisição: "Mudar Tomada churrasqueira" tem a mesma forma e é tarefa única; (3) conector "e" entre 3+ palavras simples, sem repetição — que deixa passar "Concreto muros e muros" (repete) e "Suítes 1 e 2" (número). Qualquer palavra de frase (de, para, com, no…), dígito ou medida barra a quebra. A data sai antes de julgar, e o prazo não se perde porque vive no campo da lista (D157). Guardado como regressão em `scripts/testar-enumeracao.ts`: 37 linhas da amostra real do WhatsApp têm que continuar inteiras | ✅ |
| D158 | **O checklist não vira mensagem na conversa; o estado volta para a mensagem original.** O checklist é evento próprio e entrava no feed como uma segunda bolha, logo abaixo da lista, com os mesmos itens — marcar três listas enchia a conversa de eco. Agora ele sai do feed quando a lista de origem está lá, e a mensagem que ela mandou ganha um chip "✓ 2 de 3" / "✓ tudo feito". A conversa volta a ser só o que ELA mandou (D6); o registro derivado continua existindo no banco e continua sendo o que as lentes leem. Checklist órfão (sem lista de origem) continua aparecendo, senão sumiria do app | ✅ |
| D157 | **A lista crua e o checklist usam o MESMO parser.** A prévia mostrava "areia hoje" e, ao tocar no item, virava "areia": a tela mentia sobre o que ia acontecer. Agora `itensDaLista` e o card passam por `itensParaChecklist` nos dois casos. De quebra, o prazo da lista é copiado para o checklist ao convertê-lo — sem isso, "tudo isso para amanhã" sumia no instante em que ela marcava o primeiro item | ✅ |
| D156 | **Enumeração numa linha vira vários itens:** "comprar cal, cimento e areia" são três. A porta é estreita de propósito — exige **vírgula E conector** ("A, B e C", como se enumera em português) e pedaços curtos (até 5 palavras). Vírgula sozinha aparece em frase ("pix pro José Costa, mão de obra da semana") e "e" sozinho aparece em nome ("Suítes 1 e 2", "Concreto muros e muros"): separar por um só dos dois picotava a amostra real do WhatsApp. Vírgula de número ("2,5", "R$ 1.250,00") não conta. Isto **não** revoga o anti-goal de "parsear lista em colunas" do Fluxo 1: continua uma linha = um assunto, só se reconhece que o assunto tinha três coisas. A IA recebeu a mesma instrução para o áudio, preenchendo `itens` | ✅ |
| D155 | **Lista toda marcada sai da fila e vai para "Concluídas", recolhida no fim da página.** Ela continua ali a um toque, para desmarcar o que foi marcado sem querer — o que não dá é rolar por cima do trabalho já feito para achar o que ainda falta. O contador do topo passa a contar só as listas abertas | ✅ |
| D154 | **O aviso da obra diz o nome do ITEM, nunca "Lista de 24/09".** O que faz sair de casa é "comprar cal", não a data em que a lista chegou. Cada item pendente vira um prazo próprio: com data própria, usa a dela; sem, herda o prazo da lista. Item já marcado não cobra nada, então lista concluída não aparece na tela inicial. Só uma lista sem nenhum item legível cai de volta no nome "Lista de …" | ✅ |
| D153 | **Se a data virou campo, ela sai do texto do item.** "comprar para hoje cal" vira o item "comprar cal" com chip "hoje"; "20 sacos de cimento 28/09" vira "20 sacos de cimento". O prazo já aparece do lado, e repetir dentro do texto era ruído. O corte é determinístico (`removerMencaoDeData`), não pedido à IA: sai só quando uma data foi mesmo extraída, então "para o contrapiso" e "cotovelo 3/4" passam intactos, e se sobrar texto vazio o original fica. **A mensagem original na conversa nunca é alterada** (D6) — muda só o item do checklist. Data de item igual à da lista também não repete o chip | ✅ |
| D152 | **Marcar item feito é otimista; o refresh espera a mão parar.** O check pinta na hora e a gravação vai por baixo; se o banco recusar, volta. Antes o item ficava desabilitado esperando o `update` **e** o `router.refresh()` (que recarrega a aba inteira) — numa lista de dez itens cada toque parecia travar. O refresh agora acontece uma vez só, 700 ms depois do último toque, e o card ignora a versão do servidor enquanto tiver gravação em voo, senão a resposta antiga volta por cima do que ela acabou de marcar | ✅ |
| D151 | **O card da obra tem uma segunda linha, com UMA frase — a mais urgente — e só quando há o que dizer.** Escada: atrasado > vence hoje > a IA está na dúvida > volume em aberto; sem nada, a linha não existe. Dois contadores fixos em toda obra ("3 pendências · 2 decisões") viravam parede de número: com cinco obras nada chamava atenção, que é justamente o problema a resolver. O silêncio é o sinal. A frase nomeia a coisa ("Hoje: massa corrida +2") porque o nome é o que faz sair de casa; o número sozinho só diz que existe algo. A linha é um link próprio, para **Pendências** e não para a conversa — quem lê "atrasado" quer resolver. Obra arquivada não cobra nada. Os contadores saem de três consultas planas (`in` na lista de ids), sem anexo; quando pesar, o caminho é contador materializado no banco | ✅ |
| D150 | **A aba de pendências é UMA fila.** Revisa o D61. A seção "Com prazo" mostrava, em cima, itens de checklist datados que já apareciam dentro do card da lista embaixo: dois lugares para marcar a mesma coisa feita, e marcar num não mexia no outro. Agora tudo entra numa fila só — lista, checklist e registro solto com data —, ordenada por quem cobra primeiro e, sem data, pela data de criação (mais nova primeiro). O "o que vence" passou a ser uma função única (`prazosAbertos`) usada também pela tela inicial, senão um dia a lista de obras diz "2 atrasados" e a aba mostra 3. O chip de data diz "atrasado ·" / "hoje ·" em vez de só a data | ✅ |
| D149 | **Um nome só no cartão do arquivo; a busca olha tudo; o que ela escreveu nunca é sobrescrito; o arquivo encaminhado sai com nome legível.** Pedido do cliente: nome de PDF costuma vir feio ("DOC-20260802-WA0017_final(2).pdf"), e ele quer achar pelo que escreveu ("orçamento esquadria"). (a) **Bug corrigido:** a leitura da IA gravava o resumo dela POR CIMA da legenda que ela digitou junto do arquivo (PDF e foto) — justamente as palavras que ela usaria para buscar. Agora a legenda dela fica, e o texto da IA vai para `payload.summary`; só vira legenda quando ela não escreveu nada. (b) **O cartão mostra um nome só**, na ordem: o que ela escreveu → o que a IA leu → o nome original do arquivo. Três camadas na tela deixariam a lista cheia demais; o resto está na busca. Embaixo, fornecedor · valor (o nome corta, o valor nunca). A linha de itens do orçamento saiu da tela pelo mesmo motivo. (c) **A busca procura em tudo**: legenda, resumo da IA, nome original, fornecedor, itens orçados, sem diferenciar acento ("orcamento" acha "Orçamento"; "box" acha a proposta da vidraçaria). (d) **Encaminhar** saía sempre como `comprovante.pdf`, até orçamento e contrato. Agora sai "Orçamento - Vidraçaria Porto - 10-09-2026" (tipo - fornecedor ou descrição - data, sem repetir o tipo quando a descrição já começa com ele). O arquivo guardado não muda: só a cópia que sai. (e) **PDFs antigos não são relidos:** ficam com o que já tinham (nome do arquivo ou legenda). Ele tem poucos; se precisar, dá para rodar a leitura depois | ✅ |
| D148 | **Documentação = tudo o que ela mandou que não é pagamento; PDF passa pela IA; orçamento "fechado" é estado, não gasto.** Veio do cliente: ele procurou o orçamento na Documentação e não achou (morava numa página que só se abria pelo card do Resumo, D54). Olhando o código apareceu um buraco maior: contrato, projeto, nota fiscal e ART em PDF não apareciam em **aba nenhuma**, só rolando a conversa. (a) A Documentação vira uma aba com duas metades: **Fotos** (o álbum com fases, como antes) e **Arquivos** (todo orçamento, em PDF, foto ou texto, mais todo PDF ou vídeo que não seja comprovante), com filtros Todos · Orçamentos · Outros e busca. Comprovante continua fora (D105 segue valendo para o financeiro). A página `/orcamentos` virou redirecionamento para `documentacao?ver=arquivos&filtro=orcamentos`, e o card do Resumo aponta para lá. Existe uma casa só, e o card é atalho para ela, não um segundo lugar. (b) **PDF passa pela IA** (antes só áudio e imagem; o PDF era classificado pelo nome do arquivo). O PDF é sempre **um** registro. O que decide o tipo é se o dinheiro já saiu: comprovante é gasto; orçamento, proposta ou cotação é orçamento, com fornecedor, **total** e itens; nota fiscal, contrato, projeto, ART e laudo são documento (E5). Nota fiscal é documento e não gasto porque o pagamento entra pelo comprovante, e contar os dois somaria o mesmo dinheiro duas vezes. Quando não dá para saber se foi pago, vira "unclassified" com valor e nome guardados, e o card F pergunta só "orçamento ou gasto?" (mais "outro documento"). Ela não precisa responder a pergunta a cada PDF, só na dúvida. Modelo: lite primeiro, como imagem. PDF acima de 4 MB não passa pelo limite da função e fica com a classificação pelo nome, com aviso. Testado com PDFs sintéticos: proposta de R$ 11.500 com 40% de sinal leu o total (não o sinal), o fornecedor e os 4 itens; PIX de R$ 2.750 virou gasto com o recebedor certo; contrato virou documento. (c) **"Fechei com esse" marca o orçamento como fechado (`payload.closed`) e não cria gasto.** Converter o orçamento em gasto contaria duas vezes quando o comprovante chegasse, e erraria o valor em pagamento parcelado (sinal + parcelas não são um gasto do total). O gasto continua vindo do pagamento real. (d) O "+" na metade Arquivos manda PDF **sem tipo declarado**: a IA decide, igual a mandar pela conversa. Declarar "orçamento" ali faria um comprovante entrar errado | ✅ |
| D147 | **Renomear e excluir fase a partir da Documentação.** Excluir já existia nas Configurações, mas ninguém procura lá olhando as fotos — o problema era achar o recurso. Com uma fase selecionada no filtro aparece "N fotos em X · editar fase", que abre renomear e excluir. Toque longo foi descartado: não se descobre sozinho e disputa gesto com a rolagem da barra. A confirmação diz o que acontece, porque é isso que torna seguro: `eventos.phase_id` é `on delete set null`, então o que estava na fase só perde a etiqueta e continua em "Todas" — nada é apagado. De passagem, a lixeira das Configurações excluía **num toque, sem confirmação**; agora arma e confirma | ✅ |
| D146 | **Visor de foto respeita a área segura do celular.** O visor ocupa a tela inteira, e sem `safe-area-inset` o último botão ficava embaixo da barra do iPhone e o topo embaixo do notch no app instalado — o "muito no canto" do relato. Excluir virou botão de largura cheia no fim do painel, depois das miniaturas: longe do canto, ao alcance do polegar e depois de tudo que é navegação. A confirmação são dois botões iguais, Cancelar e Excluir | ✅ |
| D144 | **Excluir foto direto do visor.** Antes era abrir a conversa, achar a mensagem, abrir o editar e excluir. Duas etapas para não apagar por toque errado, com o aviso "some daqui e da conversa" — a foto É a mensagem, e dizer isso evita surpresa. Exclusão lógica, como no resto do app | ✅ |
| D141 | **O gate de sessão tenta duas vezes antes de deslogar, e registra o motivo.** Relato: no Android o cliente era deslogado a cada fechamento do app; no iOS, não. Descartado por medição: o cookie **não** é de sessão (a biblioteca usa `maxAge` de 400 dias) e **não** é chunked (3009 bytes, um pedaço, dentro do limite). Causa provável restante: o token de acesso dura **60 minutos**, então PWA reaberto depois disso precisa **renovar pela rede** no primeiro request — e uma falha de rede ali derrubava a sessão. No iOS o app costuma ser suspenso e a renovação do cliente mantém o token fresco; no Android o processo é morto, então quem renova é o servidor. Correções: uma segunda tentativa no gate (não custa nada no caminho feliz) e um `console.warn` dizendo se **o cookie estava presente** quando barrou — é o fato que separa "problema de armazenamento" de "problema de renovação", e sem ele "desloga sozinho" é indistinguível | ✅ |
| D140 | **A foto da obra é escolhida na criação, não depois.** O círculo com a câmera era decoração: não dava para tocar, então a única forma de pôr capa era criar a obra e voltar nas configurações. Agora o círculo é o botão, com prévia local, "trocar" e "remover". Detalhe de implementação: o arquivo **só sobe depois** do insert, porque o caminho no Storage inclui o id da obra, que ainda não existe — até lá fica em memória, com a prévia num object URL. E o upload **não bloqueia a criação**: se falhar, a obra já existe e a capa entra depois pelas configurações; perder a obra por causa da foto seria trocar o essencial pelo acessório | ✅ |
| D138 | **Teto do áudio é 90s, e o motivo NÃO é timeout.** Medido: o tempo de resposta não acompanha a duração — um áudio de 66s respondeu em 4s e um de 20s em 10s; a variação é carga do modelo. O que escala com a duração é o **tamanho do corpo da requisição**, e a Vercel recusa acima de ~4,5 MB: WAV mono 16 kHz são 32 KB/s e o base64 infla 4/3, então 90s dão ~3,8 MB. O limite antigo de 180s produziria requisição que a plataforma rejeita antes de chegar ao modelo. Também **não cortamos em 30s**: um recado real de "deixa eu te passar várias coisas" levou 66s e rendeu **7 registros corretos** (2 gastos, 2 listas, 2 decisões, 1 comunicação) — cortar no meio da frase custaria mais do que esperar. Aviso visual a partir de 60s, com contagem para o fim | ✅ |
| D139 | **Entendimento do áudio pode ser refeito.** O 503 "high demand" do modelo apareceu várias vezes nos testes, e sem repetição a única saída era apagar o áudio e regravar no canteiro — absurdo, porque o arquivo nunca se perdeu, só o entendimento. A bolha sem transcrição ganha "↻ tentar entender de novo", que rebaixa o original do Storage e repete o pedido. Coerente com o D3: o áudio é o registro, a transcrição é derivada, e derivado se refaz | ✅ |
| D137 | **Encaminhar o anexo para fora do app.** Observação do cliente na demo: o comprovante sai do banco e ele manda para o pedreiro **e** para o app — trabalho duplicado. Agora o anexo tem "↗ encaminhar" na bolha da conversa, no histórico da pessoa (onde ela volta justamente para isso) e no visor de foto. Usa a **Web Share API**: abre a folha nativa, ela escolhe WhatsApp e escolhe o contato. O arquivo é baixado e reenviado como `File`, não compartilhado como link — link assinado expira e o destinatário não teria acesso. Para pagamento, a legenda já vai pronta ("Comprovante — R$ 401,00 para Valdir"), poupando digitar o valor de novo. Sem Web Share (desktop), baixa o arquivo. Sem checagem de suporte na renderização, de propósito: dependeria de estado montado e causaria descasamento de hidratação — o botão decide no toque | ✅ |
| D136 | **No comprovante, o favorecido é o RECEBEDOR — e CPF, agência e conta são ignorados de propósito.** Sem dizer isso no prompt havia risco de pegar o pagador, e não faz sentido trazer para o app dado bancário que ele não usa. Validado com um comprovante PIX real, autorizado pelo dono: R$ 401,00 e o nome do recebedor saíram corretos nos dois modelos (leve em 5,9s, bom em 16s). Limite conhecido: comprovante de dinheiro **recebido** também entraria como gasto, porque o produto só modela saída — caso de borda, editável | ✅ |
| D135 | **A ordem dos modelos depende do que entra.** Medido: em imagem o lite responde em **3,2s** contra **43,2s** do modelo bom, com a mesma classificação — e foto é a captura mais comum, então o bom sairia caro em espera e em cota sem ganho nenhum. Em áudio é o inverso: transcrição exige o bom. Logo imagem tenta o rápido primeiro, áudio tenta o bom primeiro, e o outro fica como reserva nos dois casos | ✅ |
| D131 | **Modelo: `gemini-3.5-flash`, com reserva e orçamento de tempo.** Descobertas do primeiro teste real, todas medidas: (a) `gemini-2.5-flash` responde **404 para chave nova** — foi descontinuado, então o padrão do `.env.example` estava morto ao nascer; (b) a chamada leva **20–50s**, e a Vercel corta função em 10s por padrão → `maxDuration = 60` na rota, senão o recurso funcionaria no local e morreria em produção, que é o pior tipo de bug; (c) 503 "high demand" acontece de verdade → cai para `gemini-3.1-flash-lite`, que responde mais rápido mas transcreve pior (ouviu "argamassa adesiva extra" no lugar de "AC3"), então entra só como rede de segurança; (d) cada tentativa tem teto de 35s e o total 50s, senão principal lento + reserva estouraria o `maxDuration` e a função morreria **sem responder nada** — pior que responder "não consegui" | ✅ |
| D132 | **O prompt manda REUSAR o ambiente existente.** Sem isso o modelo devolveu "Banheiro da suíte" tendo "Banheiros" na lista da obra, fragmentando o filtro da aba Decisões à toa. Mesmo motivo do reaproveitamento de favorecido (D125): o agrupamento é por nome | ✅ |
| D128 | **O áudio é convertido para WAV mono 16 kHz antes de ir para a IA.** Causa confirmada na documentação do Gemini: ele aceita só `audio/wav`, `mp3`, `aiff`, `aac`, `ogg` e `flac` — **`audio/webm` não está na lista**, e é exatamente o que o Chrome grava. O áudio subia e era guardado, mas o entendimento voltava com erro. A gravação original continua guardada como veio (pequena e toca nativamente no player); o WAV é descartável, existe só para a chamada. 16 kHz mono é o que modelo de fala quer e deixa o arquivo menor que um WAV a 48 kHz estéreo (5,6 MB contra 34 MB em 3 min). A reamostragem é feita à mão porque o Safari recusa taxa arbitrária no `OfflineAudioContext` — e o iPhone é o aparelho que precisa funcionar | ✅ |
| D129 | **Falha de transcrição diz o motivo.** "Não consegui entender o que foi dito" cobria chave ausente, arquivo recusado, resposta vazia e JSON inválido — quatro problemas diferentes com a mesma frase, o que obrigava a adivinhar. Agora cada causa tem mensagem própria e o erro cru vai para o console | ✅ |
| D130 | **Durante a gravação o composer sai de cena.** O campo de texto continuava ao lado, e dava até para digitar enquanto gravava. Uma coisa de cada vez: ou você escreve, ou você fala | ✅ |
| D124 | **Voz implementada: o áudio é guardado E entendido.** Fecha o D19/D56. O áudio vira um evento próprio (`E9_audio`, migração 002) com o arquivo e a transcrição; os registros que saem dele apontam de volta por `payload.sourceAudioEventId`. Guardar o original é exigência do D3 e não teimosia: transcrição de canteiro erra com barulho, jargão e nome próprio, e aí o play é a fonte para conferir. O card do áudio diz quantos registros gerou, senão ela não saberia que aquele áudio virou três coisas | ✅ |
| D125 | **Um áudio pode gerar VÁRIOS registros.** Até aqui era "uma mensagem = um evento"; no canteiro ela fala o pagamento e a compra pendente na mesma frase, que são lentes diferentes. O modelo recebe o contexto da obra (favorecidos, ambientes e fases já usados) para devolver "José Costa" e "Banheiros" em vez de inventar variações — o que também protege o agrupamento do resumo, que é por nome. Os derivados nascem com `edited: true`: o tipo veio do entendimento, não há o que perguntar | ✅ |
| D126 | **Motor de IA atrás de rota serverless** (`/api/entender`), como o `07_ARQUITETURA §2` já previa: a chave vive só em variável de ambiente no servidor, nunca no front — o repositório é público. A rota exige usuário autenticado **por conta própria**, além do proxy: ela gasta cota paga e não pode depender de uma camada só. Sem a chave configurada, o app segue funcionando por texto e foto, e o áudio é guardado sem transcrição | ✅ |
| D127 | **Formato de gravação negociado em tempo de execução.** O Safari do iPhone grava `mp4/aac`, Chrome grava `webm/opus`. Fixar `webm` no código deixaria o recurso morto justamente no aparelho do canteiro. Confirmado no navegador: Chromium aceita webm/opus, webm e mp4; a lista tenta nessa ordem e cai no que existir | ✅ |
| D123 | **Barra de atalho no topo da aba de pendências, fora da área que rola.** Com 14 listas a seção "Pra organizar" ficava a ~2.900px de scroll: quem só dá check em lista nunca descobria que ela existe. A solução NÃO foi encolher as listas — elas são o trabalho, e cobrar toque a mais nelas seria pior. A barra mostra as duas contas em uma linha ("14 listas · 52 em aberto" e "Pra organizar · 3 itens") e leva até a seção no toque. Nota técnica: a barra saiu do `sticky` — sticky somado a margem negativa cobria o cabeçalho "Listas" com o scroll no topo; fora do contêiner que rola, não tem como sobrepor | ✅ |
| D122 | **Na aba de pendências, lista vem antes de sugestão — e sugestão aparece uma por vez.** Três cartões verdes empilhados enchiam a tela e empurravam as listas para baixo do scroll, quando lista é justamente o que ela vem ver ali. Sugestão é o app pedindo ajuda, e pedido não passa na frente do trabalho. A seção virou "Pra organizar", mostra 1 com a contagem à vista e as outras a um toque ("ver mais 2 sugestões"). Também cumpre o D25, que pedia no máximo 1 destaque por superfície e estava sendo violado nesta tela | ✅ |
| D119 | **Registro de sugestão não engole erro.** `registrar()` devolve a falha e o cartão avisa. Foi assim que "agora não" ficou sem efeito sem ninguém ver: o banco em produção ainda tinha o `check constraint` antigo de `sugestoes.caso`, que não conhecia `H_pagamento_incompleto`, então o INSERT era rejeitado, o erro era descartado e a sugestão voltava para sempre. Migração `001_sugestoes_caso_sem_check.sql` derruba o constraint. **Probe no banco confirmou**: A–E e G eram aceitos, F e H rejeitados — exatamente os dois cartões que não sumiam | ✅ |
| D120 | **Sugestão fora da conversa cita a mensagem.** "Marcar 11/04 como prazo disso?" — disso o quê? Na conversa a mensagem está logo acima; na aba de pendências não, e o cartão perdia o referente. Agora ele mostra um trecho da mensagem com "ver no chat" ao lado | ✅ |
| D121 | **Cada obra tem ação própria na lista "Minhas obras":** engrenagem levando aos dados da obra (onde arquivar vive) e, na arquivada, um "Reabrir" direto no cartão — reativar é a razão de ela continuar listada, então não podia custar três toques. A engrenagem fica fora do link da obra, porque link dentro de link não vale em HTML e o toque abriria a conversa | ✅ |
| D116 | **Bottom sheet vai por portal no `<body>`.** Dentro de ancestral com `transform` — o card com arraste para excluir — o `position: fixed` passa a se posicionar **em relação ao card**, não à tela: o sheet abria do tamanho do card e crescia para cima. Portal tira o sheet de qualquer contexto de layout. Junto: o sheet virou coluna com altura máxima de 88dvh, conteúdo rolando por dentro e cabeçalho/ação fixos, então lista grande não estoura a tela nem esconde o "Salvar" | ✅ |
| D117 | **Lista com prazo sobe no lugar, não em seção separada.** A seção "Com prazo" repetia a lista como um segundo registro chamado "Registro", sem dizer do que se tratava. Agora a lista com prazo é ordenada para o topo da própria seção "Listas", com a data no card — e o chip fica **vermelho quando vence hoje ou já venceu**. A seção "Com prazo" continua, só para o que não é lista (registro datado e item de checklist com prazo). Menos repetição, e a priorização acontece onde a ação está | ✅ |
| D118 | **Registro sem texto tem descrição de verdade** (`descricaoDoEvento`): título do checklist, legenda, nome do arquivo ou contagem de anexos — em vez de "Registro", que não dizia nada sobre o que precisava ser feito | ✅ |
| D109 | **Data em palavras é entendida como data.** "para amanhã", "até segunda", "depois de amanhã", "em 3 dias", "semana que vem" — no canteiro é assim que se fala, e exigir "20/09" era exigir que ela traduzisse o próprio jeito de escrever. Dia da semana sempre aponta para o próximo ("até sexta" numa sexta é a sexta que vem) | ✅ |
| D110 | **Prazo declarado entra sozinho; data só citada continua sugestão.** "comprar cimento até sexta" marca o prazo sem perguntar (tem palavra de intenção: até / para o dia / prazo / previsão, ou expressão relativa). "reunião 11/04" não marca nada e vira sugestão, porque pode ser só referência. Mantém o D18 (data é oportunista) sem obrigar um toque para o caso óbvio | ✅ |
| D111 | **Pagamento novo é ligado ao favorecido que já existe.** Repetir "paguei pro Valdir" criava um segundo Valdir sem tipo no resumo, porque o agrupamento é pelo nome e nada religava ao cadastro. Agora a captura procura o favorecido pelo nome normalizado e herda o `favorecido_id` e o tipo | ✅ |
| D112 | **Campo de favorecido tem sugestão própria, não `<datalist>`.** O nativo só abre com o campo vazio e vira um botãozinho minúsculo do navegador — quem já estava digitando não recebia sugestão nenhuma. Agora a lista filtra a cada letra, mostra tipo e quantidade de pagamentos, e quando nada casa fica explícito que o nome é novo ("+ usar «X»") | ✅ |
| D113 | **Lista criada é editável:** renomear item, tirar, acrescentar e definir prazo. Item digitado errado só saía apagando a lista inteira. Editar uma lista ainda crua a converte em checklist, que é onde o item passa a ter vida própria — conversão segue invisível (D76) | ✅ |
| D114 | **Intenção declarada ganha de palavra-chave de decisão.** "preciso resolver o rejunte para amanhã" virava decisão de acabamento só por citar "rejunte". Verbo de intenção (preciso / tenho que / falta / vou) sem dois-pontos de valor é tarefa. Com valor declarado ("Rejunte do banheiro: cinza ártico"), segue decisão | ✅ |
| D115 | **Armadilha técnica registrada: `\b` não funciona depois de letra acentuada em regex JS** (é ASCII). `/\bat[ée]\b/` nunca casava com "até" e `/\bamanh[ãa]\b/` nunca casava com "amanhã" — dois recursos de data estavam mortos em silêncio. Onde o padrão termina em acento, usar `(?![a-zà-ÿ])` no lugar do `\b` final | ✅ |
| D104 | **Texto do registro é editável.** Erro de digitação obrigava a apagar e remandar. O bottom sheet edita o texto, a legenda do anexo, o tipo, a fase e os dados de pagamento. O tipo escolhido continua valendo — não reclassifico por trás ao corrigir o texto, senão a correção mudaria a aba sem avisar | ✅ |
| D105 | **Documentação é o álbum da OBRA, não o arquivo financeiro.** Comprovante de pagamento saía na galeria junto das fotos de evolução. Gasto e orçamento ficam de fora da Documentação (e das "fotos recentes" do resumo): o comprovante já tem casa no histórico da pessoa, e ali ele é evidência, não documentação de obra. *Ajustado no D148: orçamento volta à Documentação, na metade "Arquivos"; o que fica fora é só o pagamento* | ✅ |
| D106 | **Anexo espera a legenda antes de ir, como no WhatsApp.** O arquivo fica em espera no composer, ela escreve o que é (opcional) e manda tudo junto. Não é gate (D1): mandar sem escrever nada continua a um toque. E **a legenda manda na classificação** — "comprovante do Valdir 500" vira gasto com valor e favorecido, mesmo sendo uma foto; sem legenda, vale o nome do arquivo | ✅ |
| D107 | **Campo de favorecido sugere quem já recebeu na obra** (cadastro + histórico). Sem isso "José Costa", "Jose Costa" e "josé costa" viram três pessoas no resumo, porque o agrupamento é pelo nome — e o histórico racha | ✅ |
| D108 | **Cada rótulo de tipo é o nome da lente que ele alimenta.** "material" vs "pagamento" confundia com razão: tijolo comprado é material E é pagamento. O eixo real não é o assunto, é **o tempo** — o que ainda vou fazer é `pendência` (aba Pendências), o que já saiu do bolso é `gasto` (Resumo). Renomeados só os rótulos; os códigos E1–E8 do `07_ARQUITETURA` seguem iguais | ✅ |
| D102 | **Chave de arquivo no Storage é higienizada; nome real fica no payload.** O Supabase recusa chave com acento, "º" e espaço — em português isso é a regra ("Orçamento nº 335396.pdf"), e o upload falhava **em silêncio**, sem anexo e sem aviso. Agora a chave vai sem acento e o `payload.fileName` guarda o nome de verdade para exibição. Junto: anexo que não sobe passa a avisar na tela em vez de sumir | ✅ |
| D103 | **Conta de demonstração populada por script** (`scripts/semear-demo.ts`, com roteiro em `docs/roteiro-demo.md`), a partir da amostra real do export: duas obras (uma arquivada), fotos por fase, orçamento em PDF, checklist em andamento, pagamentos com prestador, fornecedor e sem tipo, e uma decisão com ambiente inventado. Testar deixa de exigir digitar tudo à mão. Credenciais nunca entram no repositório — vão por argumento. O script limpa a conta antes (inclusive os arquivos do Storage, que não caem em cascata) e distribui os registros em ~3 meses, para a obra parecer vivida; cada caso da engine de sugestão tem um gatilho semeado de propósito | ✅ |
| D95 | **Arquivar tira da lista do dia a dia, não do app.** A obra arquivada sumia por completo — mas cliente antigo volta a perguntar coisa, e o histórico é justamente o valor do produto. A home ganhou uma seção "Arquivadas" recolhida, a obra continua abrindo normalmente (com selo no header) e as Configurações têm "Reativar obra" | ✅ |
| D96 | **O header inteiro da obra leva às Configurações** — foto, nome e ⋮ são o mesmo destino. Alvo de 44px escondido atrás de três pontinhos é alvo que ninguém acha no celular | ✅ |
| D97 | **A conversa abre na mensagem mais recente**, como qualquer chat, com botão "↓" quando ela sobe no histórico. Abria na mensagem mais antiga, obrigando a rolar a obra inteira. Exceção: chegando por "ver no chat", o alvo é a mensagem do link, não o fim | ✅ |
| D98 | **Ambiente é lista aberta, não taxonomia fechada.** Os 7 conhecidos são só o que o app detecta sozinho no texto; casa real tem canil, adega, ateliê. O "+ outro" fica **junto da decisão** (ao criar e ao editar), não na barra de filtro: filtro para tag que nenhuma decisão usa só devolveria tela vazia. A barra de filtro cresce sozinha a partir do que a obra realmente usa | ✅ |
| D99 | **Editar a pessoa ≠ editar o pagamento.** Valor, data e fase são de cada pagamento (bottom sheet de registro, agora acessível também da tela da pessoa). Nome e tipo (prestador/fornecedor) são da pessoa e valem para todo o histórico — renomear reescreve o `payeeName` de todos os pagamentos dela, porque o agrupamento do resumo é feito pelo nome que está no evento | ✅ |
| D100 | **Capa da obra é editável, e o mascote é o padrão — não um erro.** Sobe para o mesmo bucket privado dos anexos, na pasta do usuário, e é exibida por URL assinada em lote (uma chamada para a lista inteira da home) | ✅ |
| D101 | **Gasto por prestador/fornecedor tem filtro por tipo**, e o seletor só aparece quando há o que separar — com todo mundo sem tipo ele seria um controle que não muda nada | ✅ |
| D94 | **Resumo e Dash fundidos numa aba só, chamada "Resumo".** O Resumo era um Dash pior: dos seus 4 contadores, 3 (gasto, a comprar, fotos) já existiam melhor no Dash, e o quarto ("loops abertos") duplicava as sugestões de Pendências. Contador que só serve de atalho para outra aba não paga seu espaço quando as abas estão no topo, a um toque. Fica o conteúdo do Dash + "Últimos registros" (a única coisa que o Resumo tinha de próprio). Bônus: 5 abas cabem na tela do celular, 6 não cabiam. A rota `/dash` vira redirecionamento permanente para não quebrar link salvo | ✅ |
| D86 | **Tempo do verbo separa pagamento de lista.** "comprei/compramos/gastei/quitei/acertei" é dinheiro que já saiu (E7); "comprar" no infinitivo continua sendo item de lista (E1). Sem isso, "comprei 200 de tijolo na construção Bom Lar" virava material e nunca chegava no dash. Trava junto: número seguido de unidade é **quantidade, não valor** ("comprei 3 sacos de cimento" não são R$ 3) | ✅ |
| D87 | **Favorecido também sai do lugar da compra.** Além de "para/pro/pra <nome>", lê "na/no/em <estabelecimento>" — mas só quando a preposição forte não achou nada, e com stoplist de lugar/tempo ("na obra", "na sexta"). Fornecedor de balcão é tão favorecido quanto prestador | ✅ |
| D88 | **Acabou a sugestão "transformar essa lista em checklist".** Desde o D74/D76 a lista já é marcável item a item e converte no primeiro toque — perguntar de novo era o app pedindo permissão para algo que já estava feito. A fusão de status (B_status) fica, porque essa sim é uma decisão real | ✅ |
| D89 | **"A comprar" conta lista crua também.** Só contava item de checklist, então uma lista recém-chegada aparecia na aba de pendências com o contador do dash dizendo zero. A regra de "lista aberta" virou módulo único (`lib/pendencias.ts`) usado por pendências, dash e resumo | ✅ |
| D90 | **O gate de autenticação valida o JWT localmente (`getClaims`), não pergunta ao servidor de Auth (`getUser`).** Medido: `getUser` custava **145–420ms em toda navegação** e era a maior fatia da lentidão ao trocar de aba; `getClaims` responde em 1–6ms verificando a assinatura contra o JWKS em cache. Sessão expirada continua sendo renovada (o `getClaims` passa pelo `getSession`), e se o projeto usasse chave simétrica ele cairia sozinho de volta no `getUser` | ✅ |
| D91 | **Lente que não mostra imagem não assina URL de anexo.** Pendências e Resumo pagavam uma ida ao Storage por troca de aba sem exibir uma foto sequer (`carregarEventosComAnexos(..., { assinar: false })`) | ✅ |
| D92 | **Linha de prestador no dash é linha clicável, não gráfico.** Com inicial, contagem de pagamentos, chevron e a dica "toque no nome para ver o histórico" — antes parecia só uma barra e ninguém descobria que abria o extrato da pessoa | ✅ |
| D93 | **Texto sobre o header verde usa cor clara.** O "Sair" estava em `--ink-soft`, que foi pensado para fundo claro, e sumia contra o `--primary` | ✅ |
| D79 | **Atalho de captura nas lentes ("+"), nunca um caminho paralelo.** O FAB da lente NÃO grava um registro próprio: abre o bottom sheet e escreve **uma mensagem na conversa**, já com o tipo declarado (`edited: true`, para não reabrir a sugestão de classificação). A lente continua só lendo (D13) e a conversa continua sendo o arquivo master (D6). Tipo por aba: Pendências→E1, Dash e Prestador→E7 (com valor/favorecido), Decisões→E3, Documentação→E4, Orçamentos→E8. Não aparece na Conversa (o composer já está ali) nem nas Configurações | ✅ |
| D80 | **Tokens do `tokens.css` são a fonte única de cor/raio/sombra/tipografia.** O `globals.css` importa o arquivo e o `@theme` do Tailwind só faz a ponte (`--color-primary: var(--primary)`). Onde CSS não chega — manifest do PWA (JSON), `theme-color` (lida antes do CSS) e cores de fase gravadas no banco — existe **um** espelho declarado, `src/lib/tokens.ts`, e nenhum outro hex no código | ✅ |
| D81 | **Logo nunca é recriado.** Vem sempre dos PNGs de `public/assets/logo/`: horizontal no login, mascote nos empty states e como avatar de obra sem foto, app-icon no manifest e como ícone do app. A rota que desenhava um "Z" em `ImageResponse` foi removida. Pendência de asset: o `logo-horizontal.png` tem fundo opaco (~#F9F7F3), diferente do `--bg-paper`, então vai como placa arredondada — uma versão com fundo transparente resolveria | ✅ |
| D82 | **Microfone existe como desenho antes de funcionar.** O botão fica no composer e, ao ser tocado, avisa que áudio está em construção (D56 ainda aberto). Melhor mostrar o caminho e assumir que falta do que esconder a intenção do produto | ✅ |
| D83 | **Toda lente tem "ver no chat"** levando à mensagem que originou o registro (`#evento-<id>`). Sem isso a lente é beco sem saída: você vê o dado e não acha onde ele nasceu | ✅ |
| D84 | **Manifest, ícones e assets de marca ficam fora do gate de autenticação.** O sistema operacional busca o manifest antes de existir sessão; com ele atrás do login, "adicionar à tela de início" não pegava o ícone | ✅ |
| D85 | **Divergências de mockup resolvidas a favor do texto** (regra do `09_ASSETS §3`): o Dash mantém as abas do D45 em vez da pílula "Resumo" do mockup; decisão só mostra miniatura quando veio com foto (a maioria é texto puro, e um quadrado cinza fixo é pior); prestador/fornecedor usa inicial, porque não existe campo de foto no modelo | ✅ |
| D71 | **Bottom sheet fecha arrastando para baixo** pela alça, além do toque fora e do ✕. O arraste fica restrito à alça/título para não brigar com o scroll do conteúdo | ✅ |
| D72 | **Excluir por arrasto tem duas etapas:** arrastar pouco trava o card aberto com o botão "Excluir" (precisa confirmar); arrastar até o fim joga fora direto. Um gesto só apagava registro sem querer, que é o erro mais caro aqui. O ponteiro só é capturado depois que o gesto vira arrasto horizontal — capturar no toque impedia clicar nos itens dentro do card | ✅ |
| D73 | **Excluir checklist religa a lista de origem.** O checklist guarda `sourceEventId`; ao excluir, o vínculo da lista é limpo. Sem isso a lista sumia das duas seções (apontava para um checklist inexistente) e não dava mais para recriar — bug encontrado em teste de uso | ✅ |
| D74 | **Item de lista é marcável direto**, sem precisar "virar checklist" antes: o primeiro toque num item cria o checklist já com aquele item feito. Marcar item a item era o comportamento esperado; um único botão para o card todo não servia | ✅ |
| D75 | **Fase da foto vira chips salvos no toque**, acima da legenda. Era um `<select>` embaixo do campo de legenda e o usuário acabou escrevendo "Fundação" na legenda, achando que tinha classificado a foto — a foto então não aparecia no filtro da fase | ✅ |
| D65 | **Percepção de velocidade é requisito, não polimento.** Com ~27 registros a troca de aba levava ~1s e parecia travada (o usuário clicou várias vezes achando que não respondeu). Medidas: `loading.tsx` com skeleton por aba, bolinha de pendente na aba clicada (`useLinkStatus`), URLs assinadas dos anexos em **uma** chamada em lote (era uma por anexo), corte de um `getUser()` redundante por navegação (o proxy já barra e a RLS já limita) e **"recebido" otimista** na conversa — a mensagem aparece no feed antes do banco confirmar, como manda o D1 | ✅ |
| D66 | **Excluir arrastando para o lado** (como conversa de WhatsApp) em listas e checklists, com limiar de 96px e fundo vermelho progressivo. Implementado com Pointer Events para atender toque e mouse no mesmo código | ✅ |
| D67 | **Prazo tem saída:** cada item com prazo tem "✓ feito" e "✕ tirar o prazo" — antes entrava na lista e não saía mais | ✅ |
| D68 | **Ambiente é detectado do texto dela**, não de taxonomia: "suíte/master/closet" → Quartos, "lavabo/box" → Banheiros, "gourmet/churrasqueira" → Cozinha etc. Um evento pode ter vários ambientes (a mensagem de rejuntes cita 5). Funciona sem precisar "fixar" a decisão — fixar só persiste/enriquece | ✅ |
| D69 | **Faixa "fase atual" na conversa**: o que for capturado dali pra frente herda a fase, sem ela taguear nada (fecha o que o `07 §3` pedia e faz a lente de documentação valer a pena) | ✅ |
| D70 | **Chip de tipo é componente único** (`ChipTipo`) usado na conversa, resumo e lentes — no resumo o tipo aparecia como texto solto, sem parecer tag. E registros do resumo/dash abrem o bottom sheet de edição | ✅ |
| D59 | **Enter = nova linha, não enviar.** Ela escreve listas de várias linhas o tempo todo; enviar é só pelo botão (ou Cmd/Ctrl+Enter em teclado físico) | ✅ |
| D60 | **Sugestões são priorizadas e limitadas.** Ordem: fusão de status → prazo → pagamento incompleto → prestador → decisão → classificar → checklist. Máximo de 3 por superfície: com dados reais a engine detecta 10+ e virava ruído | ✅ |
| D61 | *(revisado pelo D150 — a seção "com prazo" deixou de existir)* **Nada some por não aceitar sugestão:** a aba de pendências mostra as listas recebidas mesmo antes de virarem checklist (com botão "virar checklist" no próprio card), além de uma seção "com prazo" com eventos e itens datados | ✅ |
| D62 | **Registros são editáveis de qualquer lente**, não só da conversa — o dash abre o mesmo bottom sheet ao tocar num pagamento (reforça D46) | ✅ |
| D63 | **Classificador reescrito a partir do dado real** (ver D64): decisão se declara na primeira linha ("REJUNTES"), dinheiro sem verbo de pagamento é orçamento/cotação (e "$" sem "R" conta, é como ela escreve), linha que começa com quantidade é material, tarefa curta ("Requadros portas") entra como pendência, e texto longo corrido é comunicação — este último precisa ser testado antes das heurísticas de material, senão um "80 cm" no meio do parágrafo transforma reclamação em lista | ✅ |
| D64 | **`scripts/simular-uso.ts`**: simulação com amostra real do export de WhatsApp, usando a mesma lógica do app. Serve como checagem de regressão do classificador — foi ela que revelou os erros corrigidos em D63 | ✅ |
| D58 | **Mobile-first é premissa e critério de validação, não uma característica.** O app é pensado e construído como app de celular; rodar no navegador desktop é bônus. Toda tela precisa ser **validada em viewport de celular** antes de ser considerada pronta, nunca só em desktop. Regras derivadas: nenhuma ação pode depender de `hover` para aparecer (quebra no toque — já causou o bug do "editar" invisível), alvos de toque generosos, e instalável na tela inicial (PWA) para abrir sem cara de navegador | ✅ |
